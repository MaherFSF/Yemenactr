# /route-inventory-playwright

Create Playwright E2E tests that:
- Enumerate key public routes (EN + AR)
- Verify the page loads (no console errors)
- Verify any KPI number displayed exposes a "Show Evidence" affordance

Write the route list into docs/ROUTE_INVENTORY.md and keep it updated.

Open PR titled:
"Add Playwright route inventory"
