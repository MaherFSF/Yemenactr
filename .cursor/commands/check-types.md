# Check TypeScript Types

Run TypeScript type checking across the entire codebase.

```bash
pnpm typecheck
```

This will:
- Check all TypeScript files for type errors
- Verify imports and exports
- Validate type definitions
- Report any type mismatches

No output means all types are correct!
